<Page x:Class="UPtask1.Pages.ChangePasswordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UPtask1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Смена пароля"
      Style="{StaticResource PageStyle}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <Label Content="Новый пароль" HorizontalAlignment="Left" Style="{StaticResource LabelStyle}" />
            <PasswordBox x:Name="PbNewPassword" Style="{StaticResource PasswordBoxStyle}" />
            <Label Content="Подтвердите новый пароль" HorizontalAlignment="Left" Style="{StaticResource LabelStyle}" />
            <PasswordBox x:Name="PbConfirmPassword" Style="{StaticResource PasswordBoxStyle}" />
            <Button x:Name="BtnChangePassword" Content="Сменить пароль" HorizontalAlignment="Center" VerticalAlignment="Center" Click="BtnChangePassword_Click" Style="{StaticResource ButtonStyle}" />
            <TextBlock Style="{StaticResource LinkTextBlockStyle}">
                Вернуться к авторизации? 
                <Hyperlink x:Name="BackToAuthLink" TextDecorations="Underline" Style="{StaticResource HyperlinkStyle}" Click="BackToAuthLink_Click">
                    Войдите
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>