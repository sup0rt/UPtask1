<Page x:Class="UPtask1.Pages.PaymentTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UPtask1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaymentTabPage" Style="{StaticResource PageStyle}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="377*"/>
            <RowDefinition Height="73*"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" x:Name="DataGridPayment" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                  Binding="{Binding ID}" 
                                  Width="50"/>
                <DataGridTextColumn Header="Пользователь" 
                                  Binding="{Binding User.FIO}" 
                                  Width="150"/>
                <DataGridTextColumn Header="Категория" 
                                  Binding="{Binding Category.Name}" 
                                  Width="150"/>
                <DataGridTextColumn Header="Дата" 
                                  Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" 
                                  Width="100"/>
                <DataGridTextColumn Header="Название" 
                                  Binding="{Binding Name}" 
                                  Width="150"/>
                <DataGridTextColumn Header="Номер" 
                                  Binding="{Binding Num}" 
                                  Width="80"/>
                <DataGridTextColumn Header="Цена" 
                                  Binding="{Binding Price, StringFormat={}{0:C}}" 
                                  Width="100"/>
                <DataGridTemplateColumn Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="ButtonEdit" 
                                   Content="Редактировать" 
                                   ToolTip="Редактировать данные" 
                                   Click="ButtonEdit_Click"
                                    Style="{StaticResource ButtonStyle}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                   HorizontalAlignment="Stretch" Margin="10">
            <Button x:Name="ButtonAdd" 
                   ToolTip="Добавить данные" 
                   Content="Добавить" 
                   Margin="0,0,10,0" 
                   Click="ButtonAdd_Click" Style="{StaticResource ButtonStyle}"/>
            <Button x:Name="ButtonDel" 
                   ToolTip="Удалить данные" 
                   Content="Удалить" 
                   HorizontalAlignment="Right" 
                   Click="ButtonDel_Click" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>